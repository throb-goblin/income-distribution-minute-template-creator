# Working Templates

Prepared macro-free working templates are generated locally by `scripts/prepare_templates.py`.

The public WIP repository intentionally excludes generated Word template binaries. Keep `.gitkeep` in this directory so the path exists after clone.
